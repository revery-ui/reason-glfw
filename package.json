{
  "name": "esy-glfw",
  "version": "0.1.0",
  "description": "GLFW bindings for OCaml",
  "license": "MIT",
  "esy": {
    "build": [
        ["make", "build", "BUILDDIR=#{self.root}/_build"]
    ],
    "buildsInSource":"_build"
  },
  "peerDependencies": {
    "ocaml": "~4.6.0"
  },
  "dependencies": {
    "@opam/dune": "*",
    "@opam/js_of_ocaml-compiler": "*"
  },
  "devDependencies": {
    "ocaml": "~4.6.0"
  }
}
